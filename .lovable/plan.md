## Goal

Create a new public showcase page at `/showcase/makarawear` modeled on the existing `/showcase/brite` page (`src/pages/showcase/BrandSampleShowcase.tsx`), populated with the 88-image MAKARA WEAR batch generated by `info@tsimkus.lt`, **minus the 6 excluded scenes** (12 images excluded → **76 images** displayed).

## Source data

- User: `info@tsimkus.lt` → `user_id = fe45fd27-2b2d-48ac-b1fe-f6ab8fffcbfc`
- Batch window: last ~6h, all 88 jobs are `Red Sleeveless Swimsuit`, ratio `4:5`
- Storage path pattern (public bucket `workflow-previews`):
  `https://azwiljtrbtaupofwmpzb.supabase.co/storage/v1/object/public/workflow-previews/fe45fd27-2b2d-48ac-b1fe-f6ab8fffcbfc/{job_id}/0-0.jpg`

### Excluded scenes (per user)
- Cliffside Beach Walk Editorial
- Cabana Curtain Movement
- Resort Bokeh Swim Editorial
- Beach Towel Sitting Moment
- Golden Horizon Swim
- Textured Bikini Back Close-Up

Result: **76 images across ~40 distinct scene names** (most scenes have 2 variants — both kept, interleaved).

## Implementation

1. **New file** `src/pages/showcase/MakaraWearShowcase.tsx`
   - Copy `BrandSampleShowcase.tsx` structure (SEO head, LandingNav/Footer, hero, stats, gallery grid, lightbox, mid CTA, final CTA).
   - Swap the `IMAGES` array with the 76 MAKARA WEAR job URLs (paired with scene names; category set to `Editorial` for editorial scenes, `Essentials` for `Ghost Mannequin Shot`, `On-Model Front/Back/Editorial/Lifestyle`, `Movement Shot`).
   - Interleave so adjacent cards alternate between portrait/full-body/detail/location, matching the Brite layout.
   - `aspect-[4/5]` grid retained (matches the 4:5 generations).
   - Hero copy:
     - eyebrow: "For MAKARA WEAR"
     - H1: "Your swimwear, a full editorial drop"
     - sub: "From a single product shot, VOVV.AI built this complete swim campaign — 76 visuals, ready for web, social, and lookbook."
   - Stats: `76 Visuals` / `~3 min Made in` (rounded — actual batch took roughly 2–3 minutes based on timestamps; will pin to `~3 min`).
   - SEO: `noindex` (private brand sample, same as Brite).
   - Final CTA points to `/auth` and `/discover`.

2. **Route registration** in `src/App.tsx`
   - Add `<Route path="/showcase/makarawear" element={<MakaraWearShowcase />} />` next to the Brite route. Lazy-import to match existing showcase pattern.

3. **No backend, DB, or business-logic changes.** Pure presentation page reusing existing components (`LandingNav`, `LandingFooter`, `SEOHead`, `getOptimizedUrl`).

## Technical notes

- All 88 storage objects already confirmed present at `workflow-previews/{user_id}/{job_id}/0-0.jpg`.
- The 76 final image URLs + scene labels will be hard-coded into the component (same pattern Brite uses) — no runtime DB query, instant load.
- Image rendering uses `getOptimizedUrl(url, { quality: 50 })` for grid and `quality: 80` for lightbox, matching Brite.
- First 8 images use `loading="eager"`, rest lazy.

## Out of scope

- No changes to existing showcase pages.
- No new sitemap entries (page is `noindex`, same as Brite).
- No edits to generation pipeline, RLS, or storage.

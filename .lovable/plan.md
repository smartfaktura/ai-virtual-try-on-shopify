
The user wants global consistency across ALL `/app/*` interactive elements — buttons, inputs, selects, textareas, dropdowns. Right now there's drift: dashboard pills, page CTAs, modal buttons, form inputs, dropdowns all have different heights, radii, font sizes, paddings.

Let me look at the current state of the core primitives and how they're used.


The user is comparing button styles. Sidebar CTAs (Create, Open Studio, Browse Look, Top up) use a distinctive pill style — white bg, rounded-full, shimmer overlay, shadow — while the "Add Products" button on `/app/products` uses the default shadcn `<Button>` (rounded-md, primary bg, no shimmer).

Let me confirm by reading the relevant files.

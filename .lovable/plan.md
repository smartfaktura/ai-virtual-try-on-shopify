

# Default "Prompt Only" to Enabled in Add Scene Modal

## Change

### `src/components/app/AddSceneModal.tsx`
- Change the `promptOnly` state initializer from `false` to `true` (line ~39: `const [promptOnly, setPromptOnly] = useState(true)`)

One character change, one file.


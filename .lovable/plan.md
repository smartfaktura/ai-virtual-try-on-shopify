

## Replace Em Dashes with Regular Dashes

Change the em dash (`—`) to a regular hyphen (`-`) in the auth page subtitle on line 76 of `src/pages/Auth.tsx`:

**Before:** `Start with 20 free credits — no credit card required`
**After:** `Start with 20 free credits - no credit card required`

### File changed
- `src/pages/Auth.tsx` (line 76)


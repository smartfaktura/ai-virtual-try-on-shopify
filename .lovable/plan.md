# Adapt /ai-product-photography/eyewear to match bags · swimwear · activewear

Mirror the upgrades already shipped on the bags, swimwear and activewear category pages: hero video tile, "Motion · in movement" 6-up video strip, "One frame · Whole feed" Instagram screenshot section, tightened copy, and the same hide-pain-points treatment.

## Assets (copy from uploads to `src/assets/seo/`)

- `eyewear-feed.jpg` ← `user-uploads://vovv.ai_eyewear_feed.jpg`
- `eyewear-motion-1.mp4` … `eyewear-motion-6.mp4` ← `user-uploads://eyewear-{1..6}.mp4`
- Generate 6 poster JPGs (`eyewear-motion-{1..6}.jpg`) from frame 0 of each mp4 via ffmpeg — same pattern as the other categories so videos can lazy-load over a static poster.

## Code changes

### 1. `src/components/seo/photography/category/CategoryMotionShowcase.tsx`
- Import the 6 new mp4s + 6 posters as `ev1..ev6` / `ep1..ep6`.
- Extend `MotionSlug` union with `'eyewear'`.
- Add `eyewear` entry to `CLIPS_BY_SLUG`.
- Add `eyewear` entry to `COPY_BY_SLUG`:
  - eyebrow: `Motion · Frames in movement`
  - heading: `Your eyewear, brought to life`
  - sub: `Turn one frame photo into scroll-stopping motion for ads, reels and campaign film`
  - aria: `AI-generated eyewear motion clip`

### 2. `src/components/seo/photography/category/CategoryFeedShowcase.tsx`
- Import `eyewearFeed from '@/assets/seo/eyewear-feed.jpg'`.
- Extend `FeedSlug` with `'eyewear'`.
- Add `eyewear` entry to `FEED_BY_SLUG`:
  - eyebrow: `One frame · Whole feed`
  - heading: `Your entire eyewear feed from a single upload`
  - sub: `One frame photo in — a season of editorials, reels and PDP details out, on brand and on rhythm`
  - alt: `Curated Instagram-style feed of eyewear editorial portraits, vintage film campaigns and product still life — all generated from a single frame upload`

### 3. `src/components/seo/photography/category/CategoryHero.tsx`
- Import `eyewearMotion3 from '@/assets/seo/eyewear-motion-3.mp4'`.
- Add `'eyewear-motion-3': eyewearMotion3` to `HERO_VIDEO_MAP`.

### 4. `src/data/aiProductPhotographyCategoryPages.ts` — eyewear entry only
- Add `const eyewearMotion3 = 'eyewear-motion-3';` next to the other motion keys at top.
- Tighten copy to match the activewear/swimwear rhythm:
  - `h1Lead`: `AI eyewear photography,`
  - `h1Highlight`: `from a single upload`
  - `heroEyebrow`: `Editorial · Vintage · Lifestyle`
  - `heroSubheadline`: `Editorial portraits, vintage film campaigns and on-model frame stories generated by AI from one product photo`
- Replace `heroCollage` so the 2nd tile carries the video, matching the other 3 pages:
  ```
  [
    { subCategory: 'Editorial', imageId: 'beauty-closeup-oversized-eyewear-1776150210659', alt: 'AI eyewear beauty closeup oversized portrait.' },
    { subCategory: 'Video',     imageId: 'editorial-office-flash-eyewear-1776150153576',   alt: 'AI eyewear editorial office flash motion clip.', videoSrc: eyewearMotion3 },
    { subCategory: 'Vintage',   imageId: '1776102204479-9rlc0n',                            alt: 'AI eyewear sunset drive vintage editorial.' },
    { subCategory: 'UGC',       imageId: 'concrete-stair-selfie-eyewear-1776149876284',     alt: 'AI eyewear concrete stair selfie brutalist UGC.' },
  ]
  ```

### 5. `src/pages/seo/AIProductPhotographyCategory.tsx`
- Add `'eyewear'` to `hidePainAndUseCases` so the page hides PainPoints + UseCases like the other three (focus stays on the feed and motion stories).

## Out of scope

- No changes to other category pages, FAQ data, scene examples, related-categories, visual outputs, sitemap script, or admin overrides. No copy edits beyond the hero + section eyebrows/headings listed above.

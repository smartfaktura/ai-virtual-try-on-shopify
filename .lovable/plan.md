

# Fix Arrow Icon Color on Submit Button

## Change

### File: `src/pages/TryShot.tsx`

Lines 191 and 193: Change `text-foreground` to `text-white` on both the `Loader2` and `ArrowUp` icons so they're visible against the dark `bg-primary` button.


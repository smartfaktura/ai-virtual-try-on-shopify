Remove the two scene entries `Studio Lounge Pause` and `Gym Mirror Set` from the `IMAGES` array in `src/pages/showcase/SixOApparelShowcase.tsx`, and update the `STATS` "Visuals" count from `39` to `37`.
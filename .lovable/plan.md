Mirror the bags transformation on `/ai-product-photography/swimwear`. All visual components are already slug-gated and ready to extend.

## 1) Hero copy + collage (data file)

`src/data/aiProductPhotographyCategoryPages.ts` swimwear entry (lines 1143-1219):

- `h1Lead`: `AI swimwear photography,`
- `h1Highlight`: `from a single upload`
- `heroEyebrow`: `Bikinis · One-Pieces · Resort`
- `heroSubheadline`: `Resort editorials, poolside lifestyle and campaign visuals generated by AI from one product photo`
- `seoTitle` / `metaDescription`: keep current keywords, no change needed.

Collage chip renames + video tile on index 1:
```
Resort     → Resort
Lifestyle  → Video        (+ videoSrc: swimwearMotion4)
Editorial  → Editorial
Still      → Lifestyle
```
(keep current 4 imageIds; only labels + the videoSrc on index 1 change)

Add import at top of data file: `import swimwearMotion4 from '@/assets/seo/swimwear-motion-4.mp4';`

## 2) Assets — copy from user uploads

- `user-uploads://1..6-swimwear.mp4` → `src/assets/seo/swimwear-motion-1..6.mp4`
- `user-uploads://vovv.ai_swimwear_feed-1.jpg` → `src/assets/seo/swimwear-feed.jpg`

## 3) Motion showcase — extend existing component

`src/components/seo/photography/category/CategoryMotionShowcase.tsx`:

- Import the 6 swimwear clips alongside the bags clips.
- Build a `CLIPS_BY_SLUG: Record<'bags'|'swimwear', string[]>` map.
- Replace the `page.slug !== 'bags'` gate with `if (!(page.slug in CLIPS_BY_SLUG)) return null;`
- Use `CLIPS_BY_SLUG[page.slug]` for the grid.
- Section eyebrow stays slug-aware: bags = "Motion · Bags in movement", swimwear = "Motion · Resort in movement" (or current bags copy parameterised on `page.groupName`).

Same 2×2 mobile / 3×2 sm / 6-up lg layout, same IntersectionObserver + prefers-reduced-motion + saveData guards. No new design tokens.

## 4) Feed showcase — extend existing component

`src/components/seo/photography/category/CategoryFeedShowcase.tsx`:

- Import `swimwearFeed` alongside `bagsFeed`.
- Build `FEED_BY_SLUG: Record<'bags'|'swimwear', { image: string; eyebrow: string; heading: string; sub: string; chip: string; alt: string }>`.
- Swimwear copy:
  - Eyebrow: `One swimsuit · Whole feed`
  - H2: `Your entire resort feed from a single upload`
  - Sub: `One swim photo in — a season of posts, reels and PDP details out, on brand and on rhythm`
  - Top-right tag: `One upload`
  - Bottom-left chip: `12 posts · 3 reels`
- Replace `page.slug !== 'bags'` gate with `if (!(page.slug in FEED_BY_SLUG)) return null;`
- Stat row (12 posts / 3 reels / 4 carousels) reused as-is.

## 5) "One {noun} · Every shot" — bag-only override stays, add swimwear branch

`src/components/seo/photography/category/CategoryBuiltForEveryCategory.tsx`:

Add a swimwear branch alongside the existing `page.slug === 'bags'` block:
- Eyebrow: `One swimsuit · Every shot`
- H2: `Create every swim shot from one product photo`
- Sub: `Generate resort, editorial, lifestyle, on-model and UGC-style visuals in minutes — no destination shoot needed`

## 6) Section order on swimwear page

`src/pages/seo/AIProductPhotographyCategory.tsx` already renders:
```
CategoryBuiltForEveryCategory → CategoryMotionShowcase → CategoryFeedShowcase
```
No changes — both new sections will appear automatically once gates open.

## Out of scope

Other categories, sceneExamples / FAQ / useCases content (already strong), admin override slots, new design tokens, no DB changes.

## Open question

The bags hero video uses tile **index 1** (originally On-Body). For swimwear, same slot — should chip read **`Video`** (matches bags) or **`Motion`** / **`Reel`**? Default = `Video` for consistency unless you say otherwise.
## Transform /ai-product-photography/activewear (mirror of swimwear/bags)

### 1. Assets
Copy uploads:
- `1..6-activewear.mp4` → `src/assets/seo/activewear-motion-1..6.mp4`
- `vovv.ai_Activewear_feed-1-2.jpg` → `src/assets/seo/activewear-feed.jpg`

### 2. Hero copy + collage (`src/data/aiProductPhotographyCategoryPages.ts`, activewear entry ~1387–1463)
- `h1Lead: 'AI activewear photography,'`
- `h1Highlight: 'from a single upload'`
- `heroEyebrow: 'Pilates · Studio · Court'`
- `heroSubheadline: 'Pilates editorials, studio portraits and on-model sport visuals generated by AI from one product photo'`
- `heroCollage[1]` → `subCategory: 'Video'`, add `videoSrc: activewearMotion4` (import added at top)
- Keep imageId entries unchanged

### 3. `CategoryMotionShowcase.tsx`
- Add imports `av1..av6` for the 6 activewear clips
- Extend `MotionSlug` union → `'bags' | 'swimwear' | 'activewear'`
- Add `activewear: [av1..av6]` to `CLIPS_BY_SLUG`
- Add `COPY_BY_SLUG.activewear`:
  - eyebrow `Motion · Sport in movement`
  - heading `Your activewear, brought to life`
  - sub `Turn one product photo into scroll-stopping motion for ads, reels and pilates campaigns`
  - aria `AI-generated activewear motion clip`

### 4. `CategoryFeedShowcase.tsx`
- Import `activewearFeed`
- Extend `FeedSlug` → add `'activewear'`
- Add `activewear` entry:
  - eyebrow `One sport set · Whole feed`
  - heading `Your entire sport feed from a single upload`
  - sub `One activewear photo in — a month of editorials, reels and pilates studios out, on brand and on rhythm`
  - alt `Curated Instagram-style feed of activewear pilates editorials, studio portraits and aesthetic UGC — all generated from a single product upload`

### 5. `CategoryBuiltForEveryCategory.tsx`
Add activewear branch in the eyebrow / H2 / sub ternaries:
- eyebrow `One sport set · Every shot`
- H2 `Create every activewear shot from one product photo`
- sub `Generate pilates, editorial, studio, on-model and UGC-style visuals in minutes — no fitness shoot needed`

### 6. `src/pages/seo/AIProductPhotographyCategory.tsx`
Extend the hide gate so activewear also drops pain points + use cases (matches bags/swimwear pattern):
```ts
const hidePainAndUseCases = ['bags','swimwear','activewear'].includes(page.slug);
```

### Out of scope
Other categories, sceneExamples/FAQ/useCases content, admin overrides, design tokens, DB. Section order unchanged (already global — Related is last on every page).


# Widen No Credits Modal

## Change
In `src/components/app/NoCreditsModal.tsx`, increase `max-w-xl` (576px) to `max-w-2xl` (672px). This gives the 3-column plan cards more breathing room so text doesn't wrap awkwardly.

### File
- `src/components/app/NoCreditsModal.tsx` — line 71: change `max-w-xl` → `max-w-2xl`


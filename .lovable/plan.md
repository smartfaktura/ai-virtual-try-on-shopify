

## Rename Upscale Button Label

Single change in `src/components/app/LibraryDetailModal.tsx`:
- Change `"Upscale to HD"` button text to `"Enhance to 2K / 4K"`
- Update the icon from `Maximize` to `Sparkles` (already imported) to better convey enhancement

Also update the batch upscale button in `src/pages/Jobs.tsx` floating bar from `"Upscale"` to `"Enhance 2K / 4K"` for consistency.


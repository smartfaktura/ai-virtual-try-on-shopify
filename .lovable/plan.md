

## Update Hero Showcase Captions

Remove the em-dash separators from all three showcase captions for a cleaner read.

### Changes in `src/components/landing/HeroSection.tsx`

| Line | Current | New |
|------|---------|-----|
| 37 | `'Same top — ∞ environments — 12 seconds'` | `'Same top ∞ environments in 12 seconds'` |
| 51 | `'Same serum — ∞ scenes — 12 seconds'` | `'Same serum ∞ scenes in 12 seconds'` |
| 65 | `'Same ring — ∞ shots — 12 seconds'` | `'Same ring ∞ shots in 12 seconds'` |

Three string changes, single file.




## Update Freestyle Showcase Content

### Changes to `src/components/landing/FreestyleShowcaseSection.tsx`

**1. Longer, more descriptive prompt text**
- Desktop: `'White Crop Top — basketball court, studio backdrop, café setting'`
- Mobile: `'Crop Top — court, studio, café'` (still fits one line)

**2. Update chip labels**
- Product chip: mobile label `'Crop Top'` (was `'Top'`), desktop stays `'White Crop Top'`
- Scene chip: mobile label `'Select scenes'` (was `'Scene'`), desktop label `'Select scenes'` (was `'Multi-Scene'`)

**3. Swap one result card image to café**
- Replace the current 3rd card (`virtual-tryon-3.png` / "Urban") with `cafe-lifestyle.png` / "Café"
- Updated result set: Studio, Basketball Court, Café
- Labels updated to match the new prompt scenes




## Update Hero Captions — Fix Timing Claim

The "12 seconds" claim is inaccurate; generation actually takes 1-2 minutes. Update all three showcase captions.

### Changes in `src/components/landing/HeroSection.tsx`

| Current | New |
|---------|-----|
| `'Same top ∞ environments in 12 seconds'` | `'Same top ∞ environments in under 2 minutes'` |
| `'Same serum ∞ scenes in 12 seconds'` | `'Same serum ∞ scenes in under 2 minutes'` |
| `'Same ring ∞ shots in 12 seconds'` | `'Same ring ∞ shots in under 2 minutes'` |

Three string changes, single file.


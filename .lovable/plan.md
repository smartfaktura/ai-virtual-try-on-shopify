

# Rename Edit Intent Option

**File: `src/components/app/freestyle/ImageRoleSelector.tsx` (line ~22)**

Change the label for `replace_product` from `'Replace product'` to `'Replace/edit product'`.

Single-line text change.


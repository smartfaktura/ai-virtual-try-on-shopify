## Scope
Bags page only. Hero block + "One bag · Every shot" section.

## 1) Hero collage labels (data)
`src/data/aiProductPhotographyCategoryPages.ts` bags entry:
- `heroCollage[2].subCategory`: `Campaign` → `Studio Editorial`
- `heroCollage[3].subCategory`: `Hardware` → `Editorial`

## 2) Hero video tile (replaces ON-BODY, index 1)
- Extend type at line 83: add optional `videoSrc?: string` to `heroCollage` items.
- Set `heroCollage[1].videoSrc` to the imported `bags-motion-4.mp4` asset (ES module import — proper bundling).
- In `src/components/seo/photography/category/CategoryHero.tsx`, when `tile.videoSrc` is present, `HeroTile` renders a `<video autoplay muted loop playsInline preload="metadata" disableRemotePlayback>` with `object-cover` filling the same `aspect-[4/5] rounded-2xl` frame. The bottom-left `ON-BODY` chip stays.
- Honor `prefers-reduced-motion`: pause + first frame only.

## 3) Hero copy (Alt A — confirmed)
Bags entry:
- `h1Lead`: `AI handbag photography,`
- `h1Highlight`: `from a single upload`
- `heroEyebrow`: `Bags · Totes · Crossbody`
- `heroSubheadline`: `Studio, on-body and campaign visuals generated by AI from one product photo`

## 4) "One bag · Every shot" section (bags only)
`src/components/seo/photography/category/CategoryBuiltForEveryCategory.tsx` — gate by `page.slug === 'bags'`:
- Eyebrow (unchanged tokens, copy update): `One bag · Every shot` (renders uppercase via existing CSS)
- H2: `Create every bag shot from one product photo`
- Subtitle: `Generate campaign, editorial, studio, on-model, and UGC-style visuals in minutes — no photoshoot needed`

Other categories keep the current dynamic copy (`One {noun} · Every shot` / `Built for every {group} shot.` / `Every chip — real scenes from a single upload`).

## Files
- `src/data/aiProductPhotographyCategoryPages.ts` — type line 83, bags h1/eyebrow/sub, two collage labels, one videoSrc.
- `src/components/seo/photography/category/CategoryHero.tsx` — HeroTile video branch + ES import for the mp4 (or accept URL prop — final approach is ES import keyed by filename match, since data file can't ship a module import). Cleanest: data stores filename, hero imports all 6 mp4s once and resolves by filename.
- `src/components/seo/photography/category/CategoryBuiltForEveryCategory.tsx` — bag-only copy branch.

Switch to build mode and I'll ship it.



# Update Discover Page Subtitle — Make it clear content is AI-generated

## What Changes

### File: `src/pages/PublicDiscover.tsx` (lines 337-344)

Update the header section subtitle to communicate that all images are generated with VOVV.AI and invite users to try it free.

**Current:**
```
Discover
Browse curated AI fashion photography — prompts, scenes and styles
```

**New:**
```
Discover
All images generated with VOVV.AI — explore styles and start creating for free
```

Add a CTA link/button below the subtitle pointing to `/auth` (sign up) for non-logged-in users, or `/app/freestyle` for logged-in users. Something like:

```tsx
<p className="text-base text-muted-foreground max-w-lg mx-auto">
  All images on this page were generated with VOVV.AI — explore styles, scenes and prompts
</p>
<a href={user ? '/app/freestyle' : '/auth'}
   className="inline-flex items-center gap-1.5 text-sm font-medium text-primary hover:underline mt-1">
  Start generating for free →
</a>
```

Also update the SEO `description` meta tag to match.

### Summary
- 1 file, ~5 lines changed
- Subtitle clearly states images are AI-generated by VOVV.AI
- CTA link encourages sign-up / free generation


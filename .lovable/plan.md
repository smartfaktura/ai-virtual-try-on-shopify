
The user reports that on `/app` (Dashboard), when clicking a Discover item or Recent Creation, the preview modal appears constrained inside the main content container instead of overlaying the full viewport (sidebar visible on top of/around it in screenshot).

Let me investigate.

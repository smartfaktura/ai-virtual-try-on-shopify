In `src/pages/showcase/SixOApparelShowcase.tsx`:

1. Delete the mid CTA strip section (`<section>` containing "Yours would look like this — but with your products").

2. Rewrite the final dark CTA section copy (page is being sent directly to 6o Apparel as an example):
   - H2: `Ready to make this real for 6o Apparel?`
   - Subhead: `Create your account and we'll generate your full set from your own product photos`
   - Keep both buttons unchanged (Try free now → `/auth`, Explore more examples → `/discover`).
In `SubGroupSection` (`src/components/app/product-images/ProductImagesStep2Scenes.tsx`, line ~852), the subgroup eyebrow row (e.g. "EDITORIAL DRESS PORTRAITS …… 3 SELECTED Clear") sits too close to the scene cards below it.

**Change**: bump the header row's bottom margin from `mb-1` → `mb-3` so there's ~8px more breathing room between the eyebrow/Clear and the first row of scene thumbnails. No other styles or behavior change.